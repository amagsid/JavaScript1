'use strict'

// exercise 9 : Log the remainder

// If x equals 7, and the only other statement is x = x % 3, what would be the value of x after the calculation?

// Result of x after the calculation is 1.
//I assigned x the value of 7 and I used a second statement from the remainder operation and logged the result to the console
let x = 7;
x %= 3;

console.log(x)

// If y equals 21, and the only other statement is y = y % 4, what would be the value of y after the calculation?
// Result of y after the calculation is 1.
//I assigned y the value of 21 and I used a second statement from the remainder operation and logged the result to the console
let y = 21;
y %= 4;

console.log(y)


//If z equals 13, and the only other statement is z = z % 2, what would be the value of z after the calculation?
// Result of z after the calculation is 1.
//I assigned z the value of 13 and I used a second statement from the remainder operation and logged the result to the console

let z = 13;
z %= 2;

console.log(z)