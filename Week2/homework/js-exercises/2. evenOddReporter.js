'use strict'

// Step 1 Create a for loop, that iterates from 0 to 20.
for (let i = 0; i < 21; i++) {
    //console.log(i);

// Step 2 Create a conditional statement that checks if the value of the counter variable is odd or even.
    if (i % 2 == 0) {
        console.log('The number ' + i + ' is even!')
    }
    else {
        console.log('The number ' + i + ' is odd!')
    }
};
